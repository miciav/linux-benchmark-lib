---
- name: Install sysbench dependencies
  hosts: all
  become: true
  tasks:
    - name: Install sysbench package
      ansible.builtin.apt:
        name: sysbench
        state: present
        update_cache: true
