apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: {{ prometheus_namespace }}
spec:
  selector:
    app: prometheus
  ports:
    - name: web
      port: 9090
      targetPort: 9090
      nodePort: {{ prometheus_node_port }}
  type: NodePort
