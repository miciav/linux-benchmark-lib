---
- name: Install stress-ng dependencies
  hosts: all
  become: true
  tasks:
    - name: Install stress-ng package
      ansible.builtin.apt:
        name: stress-ng
        state: present
        update_cache: true
